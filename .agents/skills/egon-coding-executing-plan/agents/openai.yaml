interface:
  display_name: "EGON Coding Execute Plan"
  short_description: "Execute each Plan Step with blocking manual checks"
  default_prompt: "Use $egon-coding-executing-plan to execute an approved coding Plan one committed Step at a time, requiring every blocking Manual Check to pass before each commit and the final Spec audit."
