/**
 * 令牌与签名密钥业务域，负责访问令牌、刷新令牌和密钥生命周期。
 *
 * <p>Token and signing-key domain responsible for access tokens, refresh
 * tokens, and signing-key lifecycle management.</p>
 */
package top.egon.cola.platform.idp.admin.token;
