/**
 * OAuth2 授权域，负责客户端注册、浏览器 SSO、授权码、令牌交换和协议元数据。
 *
 * <p>OAuth2 authorization domain responsible for client registration, browser SSO,
 * authorization codes, token exchange, and protocol metadata.</p>
 */
package top.egon.cola.platform.idp.admin.oauth;
