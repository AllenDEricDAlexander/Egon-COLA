/**
 * 定义跨进程共享的 IdP Identity Directory Egon-RPC 契约和稳定服务标识。
 * 本包只描述身份事实协议边界，不包含 RBAC 权限规则或传输端实现。
 *
 * <p>Defines the cross-process IdP Identity Directory Egon-RPC contract and stable service
 * identity. This package describes only the identity-fact protocol boundary and contains
 * neither RBAC authorization rules nor transport-side implementations.</p>
 */
package top.egon.cola.platform.idp.rpc.contract;
