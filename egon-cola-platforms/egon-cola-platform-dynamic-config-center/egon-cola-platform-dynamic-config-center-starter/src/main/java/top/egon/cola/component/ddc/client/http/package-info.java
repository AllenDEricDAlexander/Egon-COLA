/**
 * DDC HTTP 客户端共享的请求规范化、签名、序列化和 RestClient 创建能力。
 * 本包不包含配置、管理或注册业务端点；包内辅助类型和常量必须只服务 HTTP 协议职责。
 *
 * <p>Shared request normalization, signing, serialization, and RestClient creation for DDC HTTP
 * clients. Business endpoints are excluded; package-private helpers and constants, when present,
 * must serve only the HTTP protocol responsibility.</p>
 */
@NonNullApi
package top.egon.cola.component.ddc.client.http;

import org.springframework.lang.NonNullApi;
