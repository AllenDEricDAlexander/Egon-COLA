/**
 * DDC 字段发现、字段绑定注册和 Spring 值重解析服务。
 * {@code DdcFieldBinding} 是仅描述绑定关系的内部协作者；本包不拉取远程配置或发布事件。
 *
 * <p>Services for DDC field discovery, binding registration, and Spring value re-resolution.
 * {@code DdcFieldBinding} is an internal binding descriptor; remote retrieval and event publication
 * are excluded.</p>
 */
@NonNullApi
package top.egon.cola.component.ddc.service.binding;

import org.springframework.lang.NonNullApi;
