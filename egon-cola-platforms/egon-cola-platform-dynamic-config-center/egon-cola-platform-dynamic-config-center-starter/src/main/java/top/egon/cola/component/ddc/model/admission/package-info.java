/**
 * DDC registration credentials are carried by the shared registry/config request models.
 * IdP OAuth2 Client owns acquisition; this legacy model package intentionally has no Java types.
 *
 * <p>Registration credentials are opaque SERVICE tokens acquired through the IdP OAuth2 Client.</p>
 */
@org.springframework.lang.NonNullApi
package top.egon.cola.component.ddc.model.admission;
