/**
 * DDC 校验和、配置格式策略、YAML 解析和服务实例元数据编解码能力。
 * 本包保持无状态并只依赖模型，不执行远程 I/O、刷新或状态持久化；服务和客户端消费这些格式能力。
 *
 * <p>Stateless DDC checksum, configuration-format strategy, YAML parsing, and service-instance
 * metadata codec support. Remote I/O, refresh, and state persistence are excluded; services and
 * clients consume these format operations.</p>
 */
@NonNullApi
package top.egon.cola.component.ddc.format;

import org.springframework.lang.NonNullApi;
