/**
 * Builds, signs, and sends Gateway interface definition reports during
 * application startup, and exposes the current reporting status.
 *
 * <p>中文：在应用启动阶段构建、签名并发送网关接口定义报告，同时提供当前上报状态。
 */
package top.egon.cola.component.gateway.starter.reporting;
