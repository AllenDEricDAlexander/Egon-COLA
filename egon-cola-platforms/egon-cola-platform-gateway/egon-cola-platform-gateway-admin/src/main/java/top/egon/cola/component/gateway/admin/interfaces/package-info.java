/**
 * Admin interfaces package.
 * <p>管理端接口包，定义管理端对外提供的控制器、请求模型和交互边界。</p>
 * <p>Use this package as the inbound interface boundary of the admin module. 使用本包作为管理模块
 * 的入站接口边界，承接外部请求并转交应用服务。</p>
 *
 * @author Egon
 */
package top.egon.cola.component.gateway.admin.interfaces;
