/**
 * Gateway MCP 核心模块的MCP子包中的MCP 订阅能力。 / This package provides MCP subscription support within the mcp subpackage in the Gateway MCP core module.
 *
 * 作用 / Purpose: 组织该包内类型的职责边界，供相邻组件按公开契约协作。 / It groups the types by responsibility so adjacent components can collaborate through their public contracts.
 *
 * 用法 / Usage: 由模块配置、应用服务或运行时适配器按需注入或调用； / Use the package through the module configuration, application services, or runtime adapters as required.
 */
package top.egon.cola.component.gateway.mcp.subscription;
