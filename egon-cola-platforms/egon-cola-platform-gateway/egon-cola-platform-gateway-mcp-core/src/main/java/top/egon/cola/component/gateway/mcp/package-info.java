/**
 * MCP gateway core package.
 * <p>MCP 网关核心包，提供 MCP 能力发现、同步与网关侧运行时协作的基础类型。</p>
 * <p>Use this package as the shared entry point for MCP gateway-core integrations and
 * runtime coordination. 使用本包作为 MCP 网关核心集成与运行时协作的共享入口。</p>
 *
 * @author Egon
 */
package top.egon.cola.component.gateway.mcp;
