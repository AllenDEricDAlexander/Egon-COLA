package top.egon.cola.platform.rbac3.admin.iam.resource.report.domain;

/** Frontend resources mechanically reported by a release pipeline. */
public enum FrontendResourceType {
    MENU,
    ROUTE,
    ACTION
}
