package top.egon.cola.platform.rbac3.admin.authorization.grant.roleresource.domain.enums;

/** Lifecycle states for a tenant-scoped direct role-resource grant. */
public enum RoleResourceGrantStatusEnum {
    ACTIVE,
    DISABLED,
    EXPIRED
}
