/**
 * top.egon.cola.platform.rbac3.admin.session.application 是 RBAC3 管理控制面 中负责应用服务和用例编排的包。
 * Package top.egon.cola.platform.rbac3.admin.session.application contains application services and use-case orchestration for the RBAC3 administration control plane.
 *
 * <p>用法：调用方通过本包暴露的类型完成对应边界内的装配、编排或扩展；具体契约以类和方法 JavaDoc 为准。
 * Usage: callers use the types in this package for assembly, orchestration, or extension within this boundary; the class and method JavaDocs define the detailed contract.</p>
 * <p>包路径语义：admin.session.application 表示该类型在 RBAC3 模块中的分层位置。
 * Package-path meaning: admin.session.application identifies the type's layer within the RBAC3 module.</p>
 */
package top.egon.cola.platform.rbac3.admin.session.application;
