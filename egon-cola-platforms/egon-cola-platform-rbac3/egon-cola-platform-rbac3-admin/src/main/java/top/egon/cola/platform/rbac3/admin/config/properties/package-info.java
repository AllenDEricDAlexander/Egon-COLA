/**
 * 提供 RBAC3 Admin config、properties 边界内的类型；调用方应只通过该边界公开契约协作。
 * Provides types within the RBAC3 Admin config.properties boundary; callers collaborate only through its exposed contracts.
 */
package top.egon.cola.platform.rbac3.admin.config.properties;
