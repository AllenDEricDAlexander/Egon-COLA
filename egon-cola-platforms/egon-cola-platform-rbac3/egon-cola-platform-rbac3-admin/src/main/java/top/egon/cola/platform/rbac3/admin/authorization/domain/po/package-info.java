/**
 * 提供 RBAC3 Admin authorization、domain、po 边界内的持久化对象类型；这些类型只服务于 Admin 内部授权实现。
 * Provides persistence-object types within the RBAC3 Admin authorization.domain.po boundary; these types serve only the Admin authorization implementation.
 */
package top.egon.cola.platform.rbac3.admin.authorization.domain.po;
