/**
 * top.egon.cola.platform.rbac3.starter.authorization 是 RBAC3 业务应用授权 Starter 中负责授权判定、策略执行和运行时上下文的包。
 * Package top.egon.cola.platform.rbac3.starter.authorization contains authorization decisions, policy enforcement, and runtime context for the RBAC3 business-application authorization starter.
 *
 * <p>用法：调用方通过本包暴露的类型完成对应边界内的装配、编排或扩展；具体契约以类和方法 JavaDoc 为准。
 * Usage: callers use the types in this package for assembly, orchestration, or extension within this boundary; the class and method JavaDocs define the detailed contract.</p>
 * <p>包路径语义：starter.authorization 表示该类型在 RBAC3 模块中的分层位置。
 * Package-path meaning: starter.authorization identifies the type's layer within the RBAC3 module.</p>
 */
package top.egon.cola.platform.rbac3.starter.authorization;
