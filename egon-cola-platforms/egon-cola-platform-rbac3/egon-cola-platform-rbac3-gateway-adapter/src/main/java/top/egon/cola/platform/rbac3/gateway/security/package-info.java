/**
 * top.egon.cola.platform.rbac3.gateway.security 是 RBAC3 Gateway 安全适配层 中负责认证、凭证和安全上下文适配的包。
 * Package top.egon.cola.platform.rbac3.gateway.security contains authentication, credentials, and security-context adapters for the RBAC3 Gateway security adapter.
 *
 * <p>用法：调用方通过本包暴露的类型完成对应边界内的装配、编排或扩展；具体契约以类和方法 JavaDoc 为准。
 * Usage: callers use the types in this package for assembly, orchestration, or extension within this boundary; the class and method JavaDocs define the detailed contract.</p>
 * <p>包路径语义：gateway.security 表示该类型在 RBAC3 模块中的分层位置。
 * Package-path meaning: gateway.security identifies the type's layer within the RBAC3 module.</p>
 */
package top.egon.cola.platform.rbac3.gateway.security;
