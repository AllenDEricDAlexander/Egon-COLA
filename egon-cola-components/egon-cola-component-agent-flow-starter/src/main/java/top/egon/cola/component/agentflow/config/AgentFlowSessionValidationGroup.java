package top.egon.cola.component.agentflow.config;

/** Validation groups for the reusable session command's create/delete operations. */
public interface AgentFlowSessionValidationGroup {

    interface Create {
    }

    interface Delete {
    }
}
