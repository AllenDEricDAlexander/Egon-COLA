spring:
  application:
    name: gateway-test-rpc-provider
  autoconfigure:
    exclude:
      - org.redisson.spring.starter.RedissonAutoConfigurationV2

server:
  port: 0
  shutdown: graceful

management:
  endpoint:
    health:
      probes:
        enabled: true

egon:
  cola:
    component:
      ddc:
        enabled: false
        registry:
          enabled: false
      rpc:
        enabled: false
        provider:
          enabled: false
      gateway:
        reporting:
          enabled: false
