/**
 * DDC 直连 RPC 客户端公共能力。
 * 本包负责编排专用直连通道，不接入普通业务 RPC 的服务发现路径。
 * / Common capabilities for direct DDC RPC clients. This package orchestrates
 * dedicated direct channels and does not join the discovery path used by
 * ordinary business RPC clients.
 */
@org.springframework.lang.NonNullApi
package top.egon.cola.component.rpc.ddc.client;
