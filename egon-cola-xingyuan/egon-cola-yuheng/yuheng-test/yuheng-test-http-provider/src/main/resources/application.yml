spring:
  application:
    name: yuheng-test-http-provider
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${TIANQUAN_SHOUBING_JWK_SET_URI:http://127.0.0.1:18120/oauth2/jwks}
  autoconfigure:
    exclude:
      - org.redisson.spring.starter.RedissonAutoConfigurationV2

server:
  port: 0
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

yuheng:
  test:
    service-version: ${YUHENG_TEST_SERVICE_VERSION:1.0.0-live}

springdoc:
  api-docs:
    enabled: true
    version: OPENAPI_3_1
  group-configs:
    - group: orders
      paths-to-match:
        - /api/orders/**
        - /api/slow/**
        - /api/fail/**
        - /api/body/**
        - /api/providers/**
    - group: inventory
      paths-to-match:
        - /api/internal/inventory/**

egon:
  cola:
    platform:
      tianquan:
        shoubing:
          service-client:
            app-id: ${YUHENG_TEST_TIANSHU_APP_ID:yuheng-test-http-provider}
            registration-id: ${YUHENG_TEST_TIANSHU_REGISTRATION_ID:yuheng-test-http-provider}
    component:
      id:
        enabled: false
      tianshu:
        enabled: false
        registration-resource-uri: ${TIANSHU_REGISTRATION_RESOURCE_URI:https://tianshu.egon.internal/local}
        biz-code: ${TIANSHU_BIZ_CODE:test-biz}
        app-code: ${TIANSHU_APP_CODE:yuheng-test-http-provider}
        env: ${TIANSHU_ENV:test}
        namespace: ${TIANSHU_NAMESPACE:yuheng-test}
        rpc:
          target: ${TIANSHU_RPC_TARGET:dns:///127.0.0.1:19080}
          load-balancing-policy: round_robin
          tls:
            development-plaintext: ${TIANSHU_RPC_DEVELOPMENT_PLAINTEXT:true}
          auth:
            runtime:
              access-key: ${TIANSHU_RPC_RUNTIME_ACCESS_KEY:}
              secret-key: ${TIANSHU_RPC_RUNTIME_SECRET_KEY:}
            registry:
              access-key: ${TIANSHU_RPC_REGISTRY_ACCESS_KEY:}
              secret-key: ${TIANSHU_RPC_REGISTRY_SECRET_KEY:}
        registry:
          enabled: false
          http:
            enabled: true
            instance-id: ${YUHENG_TEST_TIANSHU_INSTANCE_ID:${yuheng.test.provider-id:http-provider-default}}
            service-name: ${YUHENG_TEST_SERVICE_NAME:yuheng-test-http-provider}
            group: default
            version: ${yuheng.test.service-version}
            protocol: http
            advertised-host: ${yuheng.test.advertised-host:127.0.0.1}
            port: ${yuheng.test.advertised-port:0}
            lease-seconds: 30
            heartbeat-interval-seconds: 10
            fail-fast: true
            metadata:
              yuheng.zone: ${yuheng.test.zone:zone-a}
              yuheng.weight: ${yuheng.test.weight:100}
      yuheng:
        openapi:
          enabled: true
          publish-to-tianshu: true
          published-groups:
            - orders
            - inventory
          biz-code: ${YUHENG_TEST_BIZ_CODE:yuheng-test}
          application-code: yuheng-test-http-provider
          resource-uri: ${YUHENG_TEST_RESOURCE_URI:https://yuheng-test-http-provider.example.test}
          artifact-version: ${yuheng.test.service-version}
          build-id: ${YUHENG_TEST_BUILD_ID:local-http}
