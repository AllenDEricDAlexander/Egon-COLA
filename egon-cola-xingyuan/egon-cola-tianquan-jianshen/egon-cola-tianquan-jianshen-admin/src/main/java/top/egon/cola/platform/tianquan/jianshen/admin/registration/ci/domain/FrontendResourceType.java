package top.egon.cola.platform.tianquan.jianshen.admin.registration.ci.domain;

/** Frontend resources mechanically registered by a release pipeline. */
public enum FrontendResourceType {
    MENU,
    ROUTE,
    ACTION
}
