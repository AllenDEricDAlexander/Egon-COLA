spring:
  config:
    activate:
      on-profile: local-it

tianquan-jianshen:
  integration-test:
    postgres:
      url: ${TIANQUAN_JIANSHEN_IT_POSTGRES_URL}
      user: ${TIANQUAN_JIANSHEN_IT_POSTGRES_USER}
      password-file: ${TIANQUAN_JIANSHEN_IT_POSTGRES_PASSWORD_FILE}
