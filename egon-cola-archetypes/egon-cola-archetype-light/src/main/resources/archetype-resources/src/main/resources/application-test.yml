#set( $symbol_dollar = '$' )
spring:
  flyway:
    enabled: ${symbol_dollar}{FLYWAY_ENABLED:true}
    clean-disabled: false
    validate-on-migrate: true
    validate-migration-naming: true
  jpa:
    hibernate:
      # ShardingSphere does not load complete metadata from the H2 route-probe stores.
      # Flyway schema parity and routing are verified by dedicated integration tests.
      ddl-auto: none
    open-in-view: false
    show-sql: false

management:
  health:
    rabbit:
      enabled: false
    redis:
      enabled: false

dubbo:
  provider:
    export: false

app:
  datasource:
    mode: ${symbol_dollar}{APP_DATASOURCE_MODE:SHARDING}
  sharding:
    physical-data-sources:
      - name: master_data
        logical-name: master_data
        role: PRIMARY
        driver-class-name: org.h2.Driver
        jdbc-url: ${symbol_dollar}{LIGHT_SHARDING_MASTER_DATA_URL:jdbc:h2:mem:${artifactId}-test-master-data;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1}
        username: ${symbol_dollar}{LIGHT_SHARDING_USERNAME:sa}
        password: "${symbol_dollar}{LIGHT_SHARDING_PASSWORD:}"
      - name: shard_0
        logical-name: shard_0
        role: PRIMARY
        driver-class-name: org.h2.Driver
        jdbc-url: ${symbol_dollar}{LIGHT_SHARDING_SHARD_0_URL:jdbc:h2:mem:${artifactId}-test-shard-0;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1}
        username: ${symbol_dollar}{LIGHT_SHARDING_USERNAME:sa}
        password: "${symbol_dollar}{LIGHT_SHARDING_PASSWORD:}"
      - name: shard_1
        logical-name: shard_1
        role: PRIMARY
        driver-class-name: org.h2.Driver
        jdbc-url: ${symbol_dollar}{LIGHT_SHARDING_SHARD_1_URL:jdbc:h2:mem:${artifactId}-test-shard-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1}
        username: ${symbol_dollar}{LIGHT_SHARDING_USERNAME:sa}
        password: "${symbol_dollar}{LIGHT_SHARDING_PASSWORD:}"
    flyway:
      targets:
        - data-source-name: master_data
          locations:
            - classpath:db/migration/sharding/master-data
        - data-source-name: shard_0
          locations:
            - classpath:db/migration/sharding/shard
        - data-source-name: shard_1
          locations:
            - classpath:db/migration/sharding/shard
  integrations:
    rabbitmq:
      enabled: false
    redis:
      enabled: false
    external-http:
      enabled: false
