<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${package}.infrastructure.teaching.repo.dao.ClassCourseScheduleDAO">
    <sql id="scheduleColumns">
        id, tenant_id, create_user_id, create_time, update_user_id, update_time, is_deleted,
        school_class_id, course_id, starts_at, ends_at
    </sql>
    <select id="selectBySchoolClassIdOrderByStartsAt"
            resultType="${package}.infrastructure.teaching.repo.po.ClassCourseSchedulePO">
        SELECT <include refid="scheduleColumns"/> FROM class_course_schedules
        WHERE school_class_id = #{schoolClassId}
        ORDER BY starts_at
    </select>
</mapper>
