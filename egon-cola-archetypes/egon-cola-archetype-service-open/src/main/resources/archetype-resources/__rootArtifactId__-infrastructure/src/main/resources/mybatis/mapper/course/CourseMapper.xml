#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\\' )
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${package}.infrastructure.course.repo.mapper.CourseMapper">
    <resultMap id="CourseResultMap" type="${package}.infrastructure.course.repo.po.CoursePo">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="credit" property="credit"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByCode" resultMap="CourseResultMap">
        SELECT id, code, name, credit, status, created_at, updated_at
        FROM course
        WHERE code = #{code}
    </select>

    <select id="selectPage" resultMap="CourseResultMap">
        SELECT id, code, name, credit, status, created_at, updated_at
        FROM course
        ORDER BY created_at DESC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM course
    </select>
</mapper>
