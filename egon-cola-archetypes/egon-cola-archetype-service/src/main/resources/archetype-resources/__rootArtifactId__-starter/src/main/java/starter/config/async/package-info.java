#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\\' )
/**
 * Owns the starter.config.async responsibilities for the generated starter module.
 * It is the composition root and may depend on Adapter and Infrastructure.
 */
package ${package}.starter.config.async;
