<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${package}.infrastructure.user.repo.dao.UserRoleDAO">
    <resultMap id="UserRoleResultMap" type="${package}.infrastructure.user.repo.po.UserRolePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
    </resultMap>
    <select id="selectByUserId" resultMap="UserRoleResultMap">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, is_deleted,
               user_id, role_id FROM user_roles WHERE user_id = #{userId}
        ORDER BY role_id ASC, id ASC
    </select>
    <select id="selectByRoleIds" resultMap="UserRoleResultMap">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, is_deleted,
               user_id, role_id FROM user_roles WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">#{roleId}</foreach>
        ORDER BY user_id ASC, role_id ASC, id ASC
    </select>
    <select id="countByUserIdAndRoleId" resultType="long">
        SELECT COUNT(*) FROM user_roles WHERE user_id = #{userId} AND role_id = #{roleId}
    </select>
</mapper>
