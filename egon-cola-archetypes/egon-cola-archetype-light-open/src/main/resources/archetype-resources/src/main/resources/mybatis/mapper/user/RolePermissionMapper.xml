<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${package}.infrastructure.user.repo.mapper.RolePermissionMapper">
    <resultMap id="RolePermissionResultMap"
               type="${package}.infrastructure.user.repo.po.RolePermissionPO">
        <id property="roleCode" column="role_code" jdbcType="VARCHAR"/>
        <id property="permissionCode" column="permission_code" jdbcType="VARCHAR"/>
        <result property="grantedAt" column="granted_at" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="findByRoleCode" resultMap="RolePermissionResultMap">
        SELECT role_code, permission_code, granted_at
        FROM role_permissions
        WHERE role_code = #{roleCode,jdbcType=VARCHAR}
        ORDER BY permission_code
    </select>

    <select id="findByRoleCodes" resultMap="RolePermissionResultMap">
        SELECT role_code, permission_code, granted_at
        FROM role_permissions
        WHERE role_code IN
        <foreach collection="roleCodes" item="roleCode" open="(" separator="," close=")">
            #{roleCode,jdbcType=VARCHAR}
        </foreach>
        ORDER BY role_code, permission_code
    </select>

    <insert id="insertRelation"
            parameterType="${package}.infrastructure.user.repo.po.RolePermissionPO">
        INSERT INTO role_permissions (role_code, permission_code, granted_at)
        VALUES (#{roleCode,jdbcType=VARCHAR}, #{permissionCode,jdbcType=VARCHAR},
                #{grantedAt,jdbcType=TIMESTAMP})
    </insert>
</mapper>
