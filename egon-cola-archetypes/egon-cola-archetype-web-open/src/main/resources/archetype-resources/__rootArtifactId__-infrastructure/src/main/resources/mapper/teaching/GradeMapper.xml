<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${package}.infrastructure.teaching.repo.mapper.GradeMapper">
    <resultMap id="GradeResultMap" type="${package}.infrastructure.teaching.repo.po.GradePO">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectByCode" resultMap="GradeResultMap">
        SELECT id, code, name, status, created_at
        FROM grades
        WHERE code = #{code}
    </select>

    <select id="countByCode" resultType="long">
        SELECT COUNT(*) FROM grades WHERE code = #{code}
    </select>
</mapper>
