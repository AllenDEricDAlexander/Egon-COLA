<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.egon.cola.archetype.source.lightopen.infrastructure.user.dao.RoleDAO">
    <sql id="roleColumns">
        id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version,
        code, name, status
    </sql>
    <select id="selectByCode" resultType="top.egon.cola.archetype.source.lightopen.infrastructure.user.po.RolePO">
        SELECT <include refid="roleColumns"/> FROM light_roles
        WHERE deleted_at IS NULL AND code = #{code}
        ORDER BY id
    </select>
    <select id="selectActiveById" resultType="top.egon.cola.archetype.source.lightopen.infrastructure.user.po.RolePO">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, code, name, status FROM light_roles WHERE id=#{id} AND deleted_at IS NULL
    </select>
    <select id="selectActiveByIds" resultType="top.egon.cola.archetype.source.lightopen.infrastructure.user.po.RolePO">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, code, name, status FROM light_roles WHERE deleted_at IS NULL AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>
    <update id="deleteVersionedById">
        UPDATE light_roles SET deleted_at=(CURRENT_TIMESTAMP AT TIME ZONE 'UTC'), version=version+1,
            update_user_id=#{et.updateUserId}, update_time=#{et.updateTime}
        WHERE id=#{et.id} AND tenant_id=#{et.tenantId} AND deleted_at IS NULL AND version=#{MP_OPTLOCK_VERSION_ORIGINAL}
    </update>
</mapper>
