/**
 * Required two-level cache assembly: the host-side Redisson client the cache component consumes.
 * It may depend only on the Domain module.
 */
package top.egon.cola.archetype.source.agent.infrastructure.config;
