<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.egon.cola.archetype.source.agent.infrastructure.knowledge.dao.KnowledgeBaseDAO">
    <resultMap id="row" type="top.egon.cola.archetype.source.agent.infrastructure.knowledge.po.KnowledgeBasePO" autoMapping="true">
        <id column="id" property="id"/>
        <result column="chunk_config" property="chunkConfig" typeHandler="top.egon.cola.archetype.source.agent.infrastructure.knowledge.po.JsonbStringTypeHandler"/>
    </resultMap>
    <sql id="columns">id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, code, name, description, embedding_model, chunk_strategy, chunk_config, status</sql>
    <select id="selectActiveById" resultMap="row">
        SELECT <include refid="columns"/> FROM knowledge_base WHERE id = #{id} AND deleted_at IS NULL
    </select>
    <select id="selectActiveByIds" resultMap="row">
        SELECT <include refid="columns"/> FROM knowledge_base WHERE deleted_at IS NULL AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
        ORDER BY create_time DESC, id DESC
    </select>
    <update id="deleteVersionedById">
        UPDATE knowledge_base SET deleted_at = (CURRENT_TIMESTAMP AT TIME ZONE 'UTC'),
            update_user_id = #{et.updateUserId}, update_time = #{et.updateTime}, version = version + 1
        WHERE id = #{et.id} AND version = #{MP_OPTLOCK_VERSION_ORIGINAL} AND deleted_at IS NULL
    </update>
    <sql id="filters">
        deleted_at IS NULL
        <if test="keyword != null">AND (lower(code) LIKE #{keyword} OR lower(name) LIKE #{keyword})</if>
        <if test="embeddingModel != null">AND embedding_model = #{embeddingModel}</if>
    </sql>
    <select id="selectFilteredPage" resultMap="row">
        SELECT <include refid="columns"/> FROM knowledge_base WHERE <include refid="filters"/>
        ORDER BY create_time DESC, id DESC LIMIT #{size} OFFSET #{offset}
    </select>
    <select id="countFiltered" resultType="long">SELECT COUNT(*) FROM knowledge_base WHERE <include refid="filters"/></select>
    <update id="updateNameAndDescription">
        UPDATE knowledge_base SET name = #{et.name}, description = #{et.description},
            update_user_id = #{et.updateUserId}, update_time = #{et.updateTime}, version = version + 1
        WHERE id = #{et.id} AND version = #{MP_OPTLOCK_VERSION_ORIGINAL} AND deleted_at IS NULL
    </update>
</mapper>
