<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.egon.cola.archetype.source.serviceopen.infrastructure.exam.repo.dao.ScoreDAO">
    <resultMap id="ScoreResultMap" type="top.egon.cola.archetype.source.serviceopen.infrastructure.exam.repo.po.ScorePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="version" property="version"/>
        <result column="exam_id" property="examId"/>
        <result column="course_id" property="courseId"/>
        <result column="student_id" property="studentId"/>
        <result column="points" property="points"/>
        <result column="status" property="status"/>
    </resultMap>
    <select id="selectByExamIdAndId" resultMap="ScoreResultMap">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version,
               exam_id, course_id, student_id, points, status
        FROM evaluation_score
        WHERE exam_id = #{examId} AND id = #{id} AND deleted_at IS NULL
    </select>
    <select id="countByExamIdAndStudentId" resultType="long">
        SELECT COUNT(*) FROM evaluation_score
        WHERE exam_id = #{examId} AND student_id = #{studentId} AND deleted_at IS NULL
    </select>
    <select id="selectPageByExamId" resultMap="ScoreResultMap">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version,
               exam_id, course_id, student_id, points, status
        FROM evaluation_score
        WHERE exam_id = #{examId} AND deleted_at IS NULL
        ORDER BY create_time DESC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <select id="countByExamId" resultType="long">
        SELECT COUNT(*) FROM evaluation_score
        WHERE exam_id = #{examId} AND deleted_at IS NULL
    </select>
    <select id="selectActiveById" resultType="top.egon.cola.archetype.source.serviceopen.infrastructure.exam.repo.po.ScorePO">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, exam_id, course_id, student_id, points, status FROM evaluation_score WHERE id = #{id} AND deleted_at IS NULL
    </select>
    <select id="selectActiveByIds" resultType="top.egon.cola.archetype.source.serviceopen.infrastructure.exam.repo.po.ScorePO">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, exam_id, course_id, student_id, points, status FROM evaluation_score WHERE deleted_at IS NULL AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>
    <update id="deleteVersionedById">
        UPDATE evaluation_score SET deleted_at = (CURRENT_TIMESTAMP AT TIME ZONE 'UTC'),
            update_user_id = #{et.updateUserId}, update_time = #{et.updateTime}, version = version + 1
        WHERE id = #{et.id} AND version = #{MP_OPTLOCK_VERSION_ORIGINAL} AND deleted_at IS NULL
    </update>
</mapper>
