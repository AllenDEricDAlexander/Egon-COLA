spring:
  autoconfigure:
    exclude:
      - org.redisson.spring.starter.RedissonAutoConfigurationV2
  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    clean-disabled: false
    validate-on-migrate: true
    validate-migration-naming: true
management:
  health:
    rabbit:
      enabled: false
    redis:
      enabled: false


app:
  datasource:
    mode: ${APP_DATASOURCE_MODE:SHARDING}
  sharding:
    database-name: PUBLIC
    physical-data-sources:
      - name: master_data
        logical-name: master_data
        role: PRIMARY
        driver-class-name: org.h2.Driver
        jdbc-url: ${LIGHT_SHARDING_MASTER_DATA_URL:jdbc:h2:mem:egon-cola-source-light-test-master-data;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1}
        username: ${LIGHT_SHARDING_USERNAME:sa}
        password: "${LIGHT_SHARDING_PASSWORD:}"
      - name: shard_0
        logical-name: shard_0
        role: PRIMARY
        driver-class-name: org.h2.Driver
        jdbc-url: ${LIGHT_SHARDING_SHARD_0_URL:jdbc:h2:mem:egon-cola-source-light-test-shard-0;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1}
        username: ${LIGHT_SHARDING_USERNAME:sa}
        password: "${LIGHT_SHARDING_PASSWORD:}"
      - name: shard_1
        logical-name: shard_1
        role: PRIMARY
        driver-class-name: org.h2.Driver
        jdbc-url: ${LIGHT_SHARDING_SHARD_1_URL:jdbc:h2:mem:egon-cola-source-light-test-shard-1;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1}
        username: ${LIGHT_SHARDING_USERNAME:sa}
        password: "${LIGHT_SHARDING_PASSWORD:}"
    flyway:
      targets:
        - data-source-name: master_data
          locations:
            - classpath:db/migration/sharding/master-data
        - data-source-name: shard_0
          locations:
            - classpath:db/migration/sharding/shard
        - data-source-name: shard_1
          locations:
            - classpath:db/migration/sharding/shard
  integrations:
    rabbitmq:
      enabled: false
    redis:
      enabled: false
    external-http:
      enabled: false

egon:
  cola:
    component:
      rpc:
        enabled: false
        identity:
          env: ${APP_ENV:test}
          host: ${RPC_ADVERTISED_HOST:localhost}
          instance-id: ${INSTANCE_ID:${HOSTNAME:local}}
        provider:
          enabled: false
          port: ${RPC_PORT:50051}
          advertised-host: ${RPC_ADVERTISED_HOST:localhost}
          registration-fail-fast: true
        consumer:
          enabled: false
          default-timeout-ms: 3000
          max-retries: 0
        tls:
          enabled: false
          development-plaintext: true
          certificate-chain-path: ${RPC_CERTIFICATE_CHAIN_PATH:}
          private-key-path: ${RPC_PRIVATE_KEY_PATH:}
          trust-certificate-collection-path: ${RPC_TRUST_CERTIFICATE_COLLECTION_PATH:}
      ddc:
        enabled: false
        biz-code: ${DDC_BIZ_CODE:student-management}
        app-code: ${DDC_APP_CODE:${spring.application.name}}
        env: ${APP_ENV:test}
        namespace: ${DDC_NAMESPACE:default}
        registration-resource-uri: ${DDC_REGISTRATION_RESOURCE_URI:}
        redis:
          enabled: false
          host: ${DDC_REDIS_HOST:localhost}
          port: ${DDC_REDIS_PORT:6379}
          password: ${DDC_REDIS_PASSWORD:}
          database: ${DDC_REDIS_DATABASE:0}
        consistency:
          fail-fast: true
        registry:
          enabled: false
          http:
            enabled: false
            env: ${APP_ENV:test}
            namespace: ${DDC_NAMESPACE:default}
            instance-id: ${INSTANCE_ID:${HOSTNAME:local}}
            service-name: ${spring.application.name}
            version: 1.0.0
            advertised-host: ${HTTP_ADVERTISED_HOST:localhost}
            port: ${SERVER_PORT:8080}
            fail-fast: true
        rpc:
          target: ${DDC_RPC_TARGET:}
          tls:
            enabled: false
            development-plaintext: true
            certificate-chain-path: ${DDC_RPC_CERTIFICATE_CHAIN_PATH:}
            private-key-path: ${DDC_RPC_PRIVATE_KEY_PATH:}
            trust-certificate-collection-path: ${DDC_RPC_TRUST_CERTIFICATE_COLLECTION_PATH:}
          auth:
            enabled: false
            runtime:
              access-key: ${DDC_RUNTIME_ACCESS_KEY:}
              secret-key: ${DDC_RUNTIME_SECRET_KEY:}
            registry:
              access-key: ${DDC_REGISTRY_ACCESS_KEY:}
              secret-key: ${DDC_REGISTRY_SECRET_KEY:}
      gateway:
        openapi:
          enabled: false
          publish-to-ddc: false
    platform:
      idp:
        enabled: false
        register-filter: false
        issuer: ${IDP_ISSUER:}
        jwk-set-uri: ${IDP_JWK_SET_URI:}
        resource-server-id: ${IDP_RESOURCE_SERVER_ID:}
        resource-uri: ${IDP_RESOURCE_URI:}
        service-client:
          app-id: ${IDP_SERVICE_APP_ID:}
          registration-id: ddcregistration
