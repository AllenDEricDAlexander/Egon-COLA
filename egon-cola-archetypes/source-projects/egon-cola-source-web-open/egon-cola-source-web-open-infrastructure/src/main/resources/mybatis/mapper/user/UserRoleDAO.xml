<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.egon.cola.archetype.source.webopen.infrastructure.user.repo.dao.UserRoleDAO">
    <resultMap id="UserRoleResultMap" type="top.egon.cola.archetype.source.webopen.infrastructure.user.repo.po.UserRolePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="version" property="version"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
    </resultMap>
    <select id="selectByUserId" resultMap="UserRoleResultMap">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version,
               user_id, role_id FROM user_roles WHERE deleted_at IS NULL AND user_id = #{userId}
        ORDER BY role_id ASC, id ASC
    </select>
    <select id="selectByRoleIds" resultMap="UserRoleResultMap">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version,
               user_id, role_id FROM user_roles WHERE deleted_at IS NULL AND role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">#{roleId}</foreach>
        ORDER BY user_id ASC, role_id ASC, id ASC
    </select>
    <select id="countByUserIdAndRoleId" resultType="long">
        SELECT COUNT(*) FROM user_roles WHERE deleted_at IS NULL AND user_id = #{userId} AND role_id = #{roleId}
    </select>
    <select id="selectActiveById" resultType="top.egon.cola.archetype.source.webopen.infrastructure.user.repo.po.UserRolePO">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, user_id, role_id FROM user_roles WHERE id = #{id} AND deleted_at IS NULL
    </select>
    <select id="selectActiveByIds" resultType="top.egon.cola.archetype.source.webopen.infrastructure.user.repo.po.UserRolePO">
        SELECT id, tenant_id, create_user_id, create_time, update_user_id, update_time, deleted_at, version, user_id, role_id FROM user_roles WHERE deleted_at IS NULL AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>
    <update id="deleteVersionedById">
        UPDATE user_roles SET deleted_at = (CURRENT_TIMESTAMP AT TIME ZONE 'UTC'),
            update_user_id = #{et.updateUserId}, update_time = #{et.updateTime}, version = version + 1
        WHERE id = #{et.id} AND version = #{MP_OPTLOCK_VERSION_ORIGINAL} AND deleted_at IS NULL
    </update>
</mapper>
